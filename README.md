# Practice Debugging Questions

For each question, inspect the `code.c` file to locate the bug(s).  
After solving them, run the `test.sh` file to check if you've got it!

If you get a `./test.sh: Permission denied` error, run `chmod +x test.sh`.
